# AI Panel Studio 开发记录文档

---

##一、需求分析
导入题目为要求文档（`要求文档.md`），利用 Claude Code 对需求进行逐层分析，产出以下结构化交付物：

| 阶段 | 输出 | 核心关注点 |
|------|------|-----------|
| 需求理解 | `CLAUDE.md` 项目指南 | 技术选型、架构约束、开发范式 |
| 数据建模 | `docs/ER.md`（Mermaid ER 图） | 6 张核心表，UUID 主键，SQLite |
| API 契约 | `docs/API.md` | REST + SSE 端点，5 种事件类型 |
| 架构设计 | `docs/architecture.md` | 三栏布局、多讨论隔离、LLM 调度 |

**需求分析阶段的核心原则**：在写任何一行代码之前，先锁定数据模型和 API 契约。SSE 事件类型、Zustand store 结构、组件树都在编码前完成设计——这是"工程化 AI 开发"的第一道防线：用结构化文档约束 AI 的输出边界，避免自由发散。

---

```## 二、开发流程

### 2.1 三阶段开发范式

严格按照 **SDD → DDD → TDD** 顺序推进，绝不交叉：

#### 阶段 1：SDD（契约/模型驱动）— 定义真相源

- **做什么**：数据建模（Discussion、Panelist、Message、ConsensusPoint、DivergencePoint、PanelistStatusLog 六张表）、API 契约（REST + SSE）、ER 图
- **AI 角色**：资深后端架构师
- **Prompt 策略**：给定完整业务需求 → 要求输出结构化产物（字段表 + Mermaid ER 图 + SQL DDL + 设计决策）
- **验证手段**：人工审查 ER 图完整性、字段约束合理性、SSE 事件类型覆盖度
- **产出**：`docs/ER.md`、`docs/API.md`、数据库初始化脚本

#### 阶段 2：DDD（设计驱动）— 以视觉反推组件

- **做什么**：组件树设计、响应式三栏布局、深色演播厅主题、状态管理方案
- **AI 角色**：资深 UI/UX 架构师 → 前端工程师
- **Prompt 策略**：先出 ASCII 组件树 + 布局图示 → 经确认后再写代码
- **关键决策**：窄屏采用左右抽屉式（主舞台始终可见）、每个讨论独立 SSE 连接、离开讨论即断开
- **产出**：完整前端组件体系、Zustand stores、CSS 设计 Token

#### 阶段 3：TDD（测试驱动）— 先测试后实现

- **做什么**：核心逻辑（嘉宾生成、发言调度、共识提炼）→ 扩展功能（删除/置顶、流式输出）→ 全站视觉重设计 → E2E 测试
- **AI 角色**：测试工程师 → 实现工程师 → 重构工程师
- **铁律**：先写失败测试 → 验证 RED → 最小实现 → 验证 GREEN → 提交
- **产出**：42+ 单元/集成测试、5 套 Playwright E2E 测试

### 2.2 Prompt 工程实践

每个阶段的 Prompt 都遵循固定模板：

```
[角色设定]：你现在扮演一名资深 XXX 工程师
[上下文注入]：基于已确认的 XXX（数据模型/组件设计/测试用例）
[技术约束]：技术栈、架构约束、硬性要求
[输出规格]：具体产出物列表（JSON Schema / 代码 / 文档）
[验证标准]：如何判断产出合格
```

**核心经验**：AI 的输出质量取决于 Prompt 的"边界清晰度"——给定越具体的约束（文件路径、函数签名、类型定义），AI 的偏离度越低。模糊 Prompt 导致模糊代码，精确 Prompt 产生精确代码。

### 2.3 Git 提交层级

提交历史严格遵循渐进层级，体现工程化拆解过程：

```
docs:    设计文档、ER 图、API 文档、实现计划
schema:  数据模型、类型定义、数据库迁移
ui:      组件、样式、布局、视觉重设计
feat:    功能实现（路由、API 端点、业务逻辑）
fix:     Bug 修复、边界情况处理
test:    测试用例编写
```

---

## 三、AI 协同中的典型问题及解决路径

### 问题 1：多需求并行时 AI 产生幻觉，遗漏或曲解部分要求

**场景**：在 DDD 阶段搭建前端工程时，单段 Prompt 同时包含 7 项任务——脚手架搭建、依赖安装、Vite 配置、类型定义、Zustand stores、6 个核心组件、全局 CSS。AI 一次性生成了大量代码，但实际审查时发现：

- 部分组件缺少 SSE 事件处理逻辑（只写了 UI 骨架，`discussionStore` 中的 `handleSSEEvent` 函数体为空）
- Tailwind CSS v4 的 `@theme` 指令用法有误，变量无法在组件中正确引用
- 窄屏抽屉组件的 `Drawer` 缺少 `isOpen` / `onClose` 的完整状态联动，只能打开不能关闭
- 专家状态灯的颜色映射逻辑与设计文档不一致（绿色和黄色含义颠倒）

**根因分析**：当 Prompt 中包含 5 个以上的独立需求时，LLM 的注意力在长输出过程中逐渐衰减——它倾向于优先完成"骨架代码"（文件创建、import 语句、组件结构），而忽略"细节逻辑"（状态流转、边界条件、设计约束匹配）。本质上不是 AI"不理解"，而是上下文窗口内的注意力资源被多个并发需求稀释，导致后半段需求被弱化处理。

**影响**：看似一次性交付了大量代码，实际上需要逐文件审查和修复遗漏，总耗时反而超过逐个需求推进。

**解决路径**：

1. **需求拆分**：将 7 项任务拆为 3 轮独立对话——第一轮搭建脚手架 + 类型定义，第二轮实现 stores，第三轮实现组件 + CSS。每轮只给 2-3 个紧密关联的需求
2. **检查清单法**：每轮结束后，让 AI 根据原始需求生成检查清单，逐项自检是否完成（例如："请逐一检查 6 个组件是否都实现了以下功能：SSE 事件处理、中文 UI 文本、响应式布局"）
3. **增量验证**：每轮完成后立即用 Chrome DevTools 或 `pnpm dev` 运行验证，不等所有代码写完才启动——尽早暴露遗漏
4. **需求优先级标注**：在 Prompt 中显式标注"必须实现"和"可选优化"，让 AI 优先保障核心逻辑的完整性，避免在次要细节上消耗注意力

**核心教训**：**AI 的注意力是有限资源，多需求并行 = 注意力稀释 = 幻觉率上升**。单段 Prompt 中的独立需求控制在 3 个以内，超过则拆分为多轮对话。宁可多轮推进，不要一次堆砌——"一次生成"的代码量不等于"一次可交付"的代码量。

### 问题 2：讨论编排管线断裂——主持人不发言、专家停滞、状态不更新

**场景**：讨论启动后的核心流程是一条串行管线：用户确认嘉宾名单 → 后端调用 LLM 生成主持人开场白 → 推送 SSE `transcript_message` 事件 → 触发 `decideNextSpeaker` 选出发言专家 → LLM 生成专家发言 → 推送发言 + 更新专家状态 → 循环。这三个 Bug 分别对应管线的三个断裂点：

| Bug | 断裂点 | 现象 |
|-----|--------|------|
| 主持人不发言 | 讨论启动环节 | 用户点击"开始讨论"后页面无任何发言，Transcript 区空白，后续流程无法触发 |
| 专家发言停滞 | LLM 调度/生成环节 | 主持人开场后，下一位专家永远不发言，或发言到一半中断，前端一直显示"准备中" |
| 状态不实时更新 | SSE 推送/前端消费环节 | 后端已生成发言，但前端专家状态小窗仍显示"待机"，共识/分歧面板不刷新 |

**根因分析**：三个 Bug 共享同一个根因——**管线缺乏超时兜底和错误恢复机制**。具体而言：

- **主持人不发言**：`startDiscussion` 是一次性 LLM 调用，网络超时或 API 返回异常时，既没有重试也没有向前端推送错误事件，前端不知道讨论"失败"了，只能白屏等待
- **专家发言停滞**：`decideNextSpeaker` → `generateSpeechStream` 是串行两步，任一步 LLM 调用超时（DeepSeek API 偶发 30s+ 延迟）都会卡死整条管线；且 `generateSpeechStream` 的 AsyncGenerator 在 HTTP 连接中断时不会自动清理，导致 SSE 连接僵死
- **状态不实时更新**：前端 `discussionStore` 中的专家状态更新依赖 SSE 事件到达，但 SSE 连接断开后前端没有主动轮询后端状态作为兜底；同时 React 的 `useEffect` 依赖数组遗漏了部分状态字段，导致 Zustand store 已更新但组件不重渲染

**影响**：讨论一旦卡住，用户只能刷新页面重新进入，体验极差——且刷新后讨论状态可能不一致（数据库已写入部分发言，但 SSE 事件丢失）。

**解决路径**：

1. **LLM 调用超时控制**：为每次 LLM 调用设置 25s 超时（`AbortController` + `setTimeout`），超时后自动重试 1 次，仍失败则向前端推送 `discussion_error` 事件，前端展示"讨论中断，点击重试"按钮
2. **管线自愈机制**：在 `discussion.ts` 的发言循环中加入 try-catch 包裹每个专家的发言生成，单次失败跳过当前专家（推送 `panelist_status: error`），让下一位专家继续，不阻断整场讨论
3. **SSE 心跳 + 前端兜底轮询**：后端每 15s 推送 `heartbeat` 事件；前端 20s 未收到心跳则判定连接断开，自动重连；重连期间通过 REST API 拉取最新状态（`GET /api/discussions/:id/status`），确保 UI 不冻结
4. **React 状态订阅修复**：检查所有 `useEffect` 的依赖数组，确保 `discussionStore` 中与 UI 渲染相关的字段（`panelistStatuses`、`consensusPoints`、`divergencePoints`）被正确订阅；将专家状态更新从深层对象合并改为 `immer`-style 不可变替换，确保 Zustand 的引用比对能检测到变化

**核心教训**：**AI 驱动的实时系统不能把 LLM 当作可靠组件来设计**。LLM 有延迟抖动、偶发超时、输出格式变异——管线的每一环都必须有超时、重试、降级、和前端错误提示。实时性的敌人不是慢，而是"卡住后什么都不说"。

### 问题 3：AI 生成代码中的"过度工程化"倾向

**场景**：在全站视觉重设计阶段，发现部分组件被过度拆分——有的组件仅 1-2 行 JSX，抽象层级过多导致代码追踪困难。同时 AI 倾向于添加未被要求的"灵活性"（如多余的 props 接口、未使用的工具函数、过度泛化的类型定义）。

**影响**：代码库膨胀、维护成本增加、组件树追踪困难。

**解决路径**：

1. **明确约束 Prompt**：在 DDD 阶段 Prompt 中加入"遵循够用原则，不引入未被要求的抽象"、"组件粒度：能在一个文件内完成的逻辑不要拆分为两个组件"
2. **代码审查回合**：每个阶段完成后用 Claude Code 的 `/code-review` 命令进行审查，专门标记过度抽象、未使用导出、可合并的薄组件
3. **合并薄组件**：将只有 1-2 行 JSX 的包装组件合并回父组件，删除未被调用的 props 接口
4. **CLAUDE.md 行为准则**：加入"禁止对单次使用代码创建抽象"、"禁止为不可能场景编写错误处理"等约束

**核心教训**：AI 模型的默认行为偏向"防御性编程"和"可扩展性"，这在大项目中是优点，但在 MVP 阶段是负担。**必须通过 Prompt 约束和代码审查对抗这种倾向**，保持代码库的"恰好够用"状态。

---

## 四、对"工程化 AI 开发"的理解

### 4.1 定义

"工程化 AI 开发"不是"用 AI 写代码"，而是**将 AI（LLM）作为一个不可靠但高产能的协作者纳入软件工程流程，用工程手段管理系统熵增、控制输出质量、保障交付确定性**。

### 4.2 核心原则

#### 原则 1：上下文是稀缺资源，必须结构化分配

LLM 的上下文窗口是有限的，且注意力随上下文增长而衰减。工程化 AI 开发的核心动作是"上下文工程"：

- **分层注入**：不把全部需求一次性灌入 Prompt，而是按阶段逐层展开（先模型 → 再契约 → 再组件 → 最后测试）
- **产物驱动**：每个阶段的输出是下一阶段的输入约束（ER 图 → API 文档 → 组件设计 → 代码实现），形成依赖链
- **CLAUDE.md 作为持久上下文**：将项目规范、技术栈、架构约束写入 `CLAUDE.md`，每次会话自动加载，减少重复注入

#### 原则 2：AI 输出不可靠，必须建立校验闭环

这是与传统软件工程最大的区别——传统开发中，工程师的输出基本可靠；AI 开发中，每行代码都可能包含幻觉、过度抽象或逻辑漏洞。因此必须建立多层校验：

| 校验层 | 手段 | 覆盖范围 |
|--------|------|---------|
| 类型系统 | TypeScript strict + Zod runtime 校验 | 所有数据结构 |
| 单元测试 | Vitest，TDD 驱动 | 核心逻辑 |
| 集成测试 | Vitest + Mock LLM Client | 服务层 |
| E2E 测试 | Playwright + Mock API 服务器 | 用户流程 |
| 人工审查 | 代码审查、UI 验收 | 设计还原度、架构合理性 |

#### 原则 3：分阶段推进，用交付物做门控

"一键生成"的失败模式不是技术上的，而是工程上的——当所有代码一次性生成，没有任何中间检查点，问题只能在最终运行时暴露，而此时修复成本最高。

工程化 AI 开发用**阶段性交付物作为门控**：
- SDD 完成 = ER 图 + API 文档经审查通过 → 才进入 DDD
- DDD 完成 = 组件树 + 布局方案经审查通过 → 才进入 TDD
- TDD 完成 = 所有测试 GREEN + E2E 通过 → 才视为完成

每一阶段都是一道质量闸门，阻止上游问题向下游扩散。

#### 原则 4：Prompt 即代码，必须版本化管理

Prompt 不是一次性的对话文本，它是驱动 AI 行为的"源代码"。工程化处理意味着：

- 所有核心 Prompt 存档到 `prompts/` 目录
- 记录每段 Prompt 的意图、遇到的挑战、修正路径
- Prompt 的迭代与代码修改同步进行——当某个模块的逻辑变化时，对应的 Prompt 也需要更新

### 4.3 本项目中的实践总结

在 AI Panel Studio 的开发过程中，"工程化 AI 开发"具体体现为：

1. **三阶段绝不交叉**：SDD 阶段不写任何 UI 代码，DDD 阶段不修改数据模型，TDD 阶段不在测试通过前添加新功能
2. **Prompt 结构化模板**：每段 Prompt 包含角色设定、上下文注入、技术约束、输出规格、验证标准五个部分
3. **依赖注入做测试隔离**：`LLMClient` 接口通过依赖注入，测试用 `MockLLMClient`，集成测试探测真实 API Key，无 Key 自动 skip
4. **Git 提交体现工程拆解**：`docs:` → `schema:` → `ui:` → `feat:` → `fix:` 的渐进层级，每一步都可追溯、可回滚
5. **降级策略明确**：每个 LLM 调用点都有失败处理路径——重试（嘉宾生成）、降级（共识提炼返回空数组）、中断（讨论创建失败）

**最终结论**：工程化 AI 开发不是让 AI 替代工程师，而是让工程师从"代码生产者"转变为"质量守护者"——核心能力从"怎么写代码"转变为"如何约束 AI 写出正确的代码、如何验证 AI 写的代码是正确的"。
